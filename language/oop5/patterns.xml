<?xml version="1.0" encoding="iso-8859-9"?>
<!-- $Revision: 1.1 $ -->
<sect1 id="language.oop5.patterns">
 <title>Modeller (Patterns)</title>
 <para>
  Modeller, en iyi uygulamaları ve doğru tasarımları betimleme yöntemidir.
  Modeller ile genel programlama sorunlarına esnek çözümler aranmaktadır.
 </para>

 <sect2 id="language.oop5.patterns.factory">
  <title>Factory</title>
  <para>
  Factory modeli, çalışma zamanında (runtime) nesnelerin yaratılmasını sağlar.
  Factory modeli olarak adlandırılmasının sebebi, nesnenin "üretiminden" sorumlu
  olmasıdır. Parametrelendirilmiş Factory, sınıfın ismini argüman olarak alarak
  nesneyi oluşturur.
  </para>
  <example>
   <title>Parametrelendirilmiş Factory Metodu</title>
   <programlisting role="php">
<![CDATA[
<?php
class Example
{
    // Parametrelendirilmiş factory metodu
    public static function factory($type)
    {
        if (include_once 'Drivers/' . $type . '.php') {
            $classname = 'Driver_' . $type;
            return new $classname;
        } else {
            throw new Exception ('Sürücü bulunamadı');
        }
    }
}
?>
]]>
   </programlisting>
   <para>
   Bir sınıf içerisinde bu metod tanımlanarak, sürücülerin çalışma esnasında
   yüklenmesi sağlanır. <literal>Example</literal> sınıfı bir veritabanı soyutlama
   sınıfı (database abstraction class) olsaydı, <literal>MySQL</literal> ve
   <literal>SQLite</literal> sürücülerinin yüklenmesi işlemi aşağıdaki biçimde
   yapılabilirdi:
   </para>
   <programlisting role="php">
<![CDATA[
<?php
// MySQL sürücüsünü yükle
$mysql = Example::factory('MySQL');

// SQLite sürücüsünü yükle
$sqlite = Example::factory('SQLite');
?>
]]>
   </programlisting>
  </example>
 </sect2>

 <sect2 id="language.oop5.patterns.singleton">
  <title>Singleton</title>
  <para>
  Singleton modeli, bir sınıfın tek bir örneğine ihtiyaç duyulduğu durumlarda
  uygulanır. Bu duruma en genel örnek, veritabanı bağlantısıdır. Bu model kullanılarak,
  tek bir örneğin başka birçok nesne tarafından erişilebilir olması sağlanabilir.
  </para>
  <example>
   <title>Singleton Fonksiyonu</title>
   <programlisting role="php">
<![CDATA[
<?php
class Example
{
    // Sınıfın bir örneğini taşır
    private static $instance;
    
    // Private olarak tanımlanmış bir ilklendirici, nesnenin direk olarak yaratılmasını engeller
    private function __construct() 
    {
        echo 'İlklendirildim';
    }

    // Singleton metodu
    public static function singleton() 
    {
        if (!isset(self::$instance)) {
            $c = __CLASS__;
            self::$instance = new $c;
        }

        return self::$instance;
    }
    
    // Örnek method
    public function bark()
    {
        echo 'Woof!';
    }

    // Kullanıcıların örneği klonlamasını engelle
    public function __clone()
    {
        trigger_error('Klon yaratılmasına izin verilmez.', E_USER_ERROR);
    }

}

?>
]]>
   </programlisting>
   <para>
   Aşağıdaki örnek <literal>Example</literal> sınıfı için tek bir örnek
   alınmasını sağlar.
   </para>
   <programlisting role="php">
<![CDATA[
<?php
// Bu işlem başarısız olur çünkü ilklendirici private olarak tanımlandı
$test = new Example;

// Bu işlem her zaman sınıfa ait tek bir örnek verir
$test = Example::singleton();
$test->bark();

// Bu bir E_USER_ERROR mesajı oluşturur.
$test_clone = clone($test);

?>
]]>
   </programlisting>
  </example>
 </sect2>

</sect1>
 
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
